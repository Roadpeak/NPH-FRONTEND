# The NHP-BACKEND API. Separate service, separate repo — the only coupling
# between them is this URL and the /api/v1 contract.
NEXT_PUBLIC_API_URL="http://localhost:4400/api/v1"

# Development shortcut until auth lands. Get this from `pnpm seed:demo` in
# the backend repo.
